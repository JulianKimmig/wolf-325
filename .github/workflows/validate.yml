name: Validate integration

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions: {}

jobs:
  validate-hacs:
    name: HACS
    uses: ./.github/workflows/hacs.yaml

  validate-hassfest:
    name: hassfest
    uses: ./.github/workflows/hassfest.yaml
